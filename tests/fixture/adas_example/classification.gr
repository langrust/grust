component classification(rgb_images: [int; 10], regions_of_interest: int) {
    x: int = rgb_images.fold(
        regions_of_interest,
        |acc: int, n: int| acc + n
    );
    out classification: [int; 10] = [x; 10];
}