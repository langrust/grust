// @pre = 0 < i1 /\ i1 < i2
// @post = o >= 0 /\ x > 0
// @state = z > 0
node test(i1: int, i2: int) {
    x: int = i2 - i1;
    y: int = i1 / x;
    z_prime: int = z + x;
    z: int = 1 fby if z_prime > 1000 then 1 else z_prime;
    out o: int = y / z;
}
