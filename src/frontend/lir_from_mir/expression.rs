use crate::lir::expression::Expression as LIRExpression;
use crate::mir::expression::Expression;

/// Transform MIR expression into LIR expression.
pub fn lir_from_mir(_expression: Expression) -> LIRExpression {
    todo!()
}
